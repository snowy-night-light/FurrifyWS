spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
  application:
    name: 'furrify-storage-service'
  main:
      web-application-type: servlet
  jpa:
    open-in-view: false
  profiles:
    active: prod
server:
  port: 8081

